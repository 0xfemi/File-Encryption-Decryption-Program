# File-Encryption-Decryption-Program
A simple yet secure Python-based file encryption and decryption tool built for cybersecurity certification projects. It uses AES-based symmetric encryption (via Fernet) from the cryptography library to protect sensitive files in any directory.
